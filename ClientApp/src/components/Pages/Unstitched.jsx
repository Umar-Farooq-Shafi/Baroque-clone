import * as React from 'react';
import { connect } from 'react-redux';


const Unstitched = () => {
    return (
        <>
            <h1>
                Unstitched
            </h1>
        </>
    );
};

export default connect()(Unstitched);
